appId: com.example.nubo_app
env:
  USERNAME: ${email || "adriantest132@protonmail.com"}
  PASSWORD: ${password || "Helper132#"}
---
- launchApp:
    clearState: true
- tapOn: Ingresar
- extendedWaitUntil:
    visible: Iniciar Sesión
    timeout: 10000
- assertVisible: Iniciar Sesión
- tapOn: Correo
- inputText: ${email}
- tapOn: Contraseña
- inputText: ${password}
- tapOn:
    text: Iniciar Sesión
    index: 1
- extendedWaitUntil:
    visible: Mascota Nubo saludando
    timeout: 10000
- assertVisible: Mascota Nubo saludando
